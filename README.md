# React Lists

Учебный Todo-лист на React.

Реализовано:
- рендер списка задач через `.map()`;
- `id` используется как `key`;
- текст `Список пуст`, если задач нет;
- добавление задачи;
- отметка задачи выполненной;
- удаление задачи;
- обновление состояния без `push()` и `splice()`.
